// Dead file — safe to delete. TrustBar was removed outright (its facts all
// live elsewhere on the page) and CraftAuthority was replaced by
// AuthorityPanorama in home-dimension.tsx. Nothing imports from here anymore.
export {};
